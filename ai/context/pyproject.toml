[project]
name = "ai-context"
version = "0.1.0"
description = "Local vector context DB for AI-assisted development (ChromaDB + fastembed + MCP)"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.5",
    "fastembed>=0.3",
    "fastmcp>=2.0",
]
